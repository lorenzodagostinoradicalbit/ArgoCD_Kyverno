apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Values.groups.kubernetesPodExecutors.gcpGroupName }}
rules:
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - exec