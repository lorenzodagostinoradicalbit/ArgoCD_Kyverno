global:
  environment: local
  targetRevision: develop
  storageClass: default
  images:
    pullPolicy: IfNotPresent
    pullSecrets: []

kyverno:
  engine:
    replicaCount: 1
  reportsControl:
    enabled: true
    ui:
      enabled: true
      useKyvernoPlugin: true